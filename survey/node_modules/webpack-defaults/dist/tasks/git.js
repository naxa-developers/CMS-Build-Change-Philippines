'use strict';

var _require = require('mrm-core'),
    lines = _require.lines;

var gitignore = [
// Logs
'logs', '*.log', 'npm-debug.log*', '.eslintcache',

//
'/coverage', '/dist', '/local', '/reports', '/node_modules',

// Editor & OS
'.DS_Store', 'Thumbs.db', '.idea', '.vscode', '*.sublime-project', '*.sublime-workspace'];

var gitattributes = ['package-lock.json -diff', '* text=auto', 'bin/* eol=lf'];

module.exports = function () {
  lines('.gitignore').add(gitignore).save();

  lines('.gitattributes').add(gitattributes).save();
};