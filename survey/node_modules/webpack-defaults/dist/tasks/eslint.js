'use strict';

var _require = require('mrm-core'),
    json = _require.json,
    lines = _require.lines;

module.exports = function () {
  // .eslintrc: overwrite
  json('.eslintrc').set({
    extends: 'webpack'
  }).save();

  // .eslintignore
  lines('.eslintignore').add(['/node_modules', '/dist']).save();
};