'use strict';

var path = require('path');

var _require = require('mrm-core'),
    copyFiles = _require.copyFiles;

// These files will be overwritten without any confirmation


var files = ['.travis.yml', 'LICENSE', '.github/ISSUE_TEMPLATE.md', '.github/PULL_REQUEST_TEMPLATE.md'];

// These files will by created only once
var filesOnce = ['src/index.js', 'src/cjs.js', 'CHANGELOG.md'];

module.exports = function () {
  var templatesDir = path.resolve(__dirname, '../../templates');
  copyFiles(templatesDir, files);
  copyFiles(templatesDir, filesOnce, { overwrite: false });
};