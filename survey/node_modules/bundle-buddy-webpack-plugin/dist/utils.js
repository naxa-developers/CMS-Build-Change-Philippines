'use strict';

var asciify = require('asciify-pixel-matrix');
var samsaccone = asciify(require('../assets/sam.json').pixels, { bg: true });

module.exports = function () {
    console.log("\n"); // eslint-disable-line
    console.log(samsaccone); // eslint-disable-line
};